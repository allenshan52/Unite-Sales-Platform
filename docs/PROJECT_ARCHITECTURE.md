# 项目架构与文件职责

## 2026-08 全栈架构与性能审查优化

- `20260820_0018_production_stability.py` 为管理员增加失败次数、锁定截止与最近登录时间，为服务端会话增加 CSRF 哈希和过期索引，并为单位增加软归档时间、归档查询索引与 SQLAlchemy 乐观锁版本。迁移会主动清空旧会话，升级后管理员需重新登录一次；单位批量核验、归档、恢复和分配负责人均在 `SELECT ... FOR UPDATE` 单事务内完成，最多处理 100 条，任何失败都整批回滚。
- `20260824_0019_remove_salesperson_coverage_radius.py` 删除已无绘图用途的 `salesperson.coverage_radius_km` 及范围约束；同行与销售地图只保留 Pin，公开 DTO 不再传输圆形中心或半径参数。
- `20260824_0020_competitor_deal_product_fields.py` 为同行逐笔成交增加产品名称、规格型号、产品图片和产品单价；项目名称、项目总价、中标时间及来源继续复用原字段。迁移仅为明确标记的虚构交易确定性补值，并保留部分无图记录覆盖情报不完整场景。
- `20260825_0021_authorized_user_roles.py` 把原单管理员账号表扩展为“普通员工 / 管理员”两级站点授权账号，现有环境变量管理员自动保留为管理员。主站登录和后台登录复用 HTTP-only 服务端会话，所有 `/api/v1/public/*` 业务接口现在也要求有效账号；健康检查和登录接口保持无认证可用。
- `20260825_0022_unite_heatmap_demo_data.py` 新增 12 家明确标记的虚构优纳特成交单位、18 笔成交合同和 14 条有效采购意向。迁移后演示库共有 27 笔优纳特成交覆盖 18 省、20 条有效意向覆盖 15 省；各省合同总金额覆盖五档热力层级。优纳特成交详情统一展示 `sales_project.contract_amount` 合同总金额，不虚构产品单价。
- `20260825_0024_insights_sales_date_index.py` 为 `sales_project.signed_at` 增加独立索引，支撑数据洞察按全年、季度和月份进行日期范围聚合；区域比较以固定批量查询读取基期，不随省市数量产生 N+1 数据库往返。
- `20260826_0025_rebalance_heatmap_demo_data.py` 仅把 0023 中八家带 `data_insights_demo` 标记的第二客户从山西、内蒙古、贵州、西藏、甘肃、青海、宁夏、新疆迁到大连、青岛、深圳、厦门、苏州、宁波、秦皇岛、北海；24 笔订单的金额、日期和关联保持不变，西部原省份仍各保留一家客户及三年订单。迁移逐行校验演示标记和预期源省份，失败整批回滚，并可完整恢复原地址与坐标。
- `20260827_0026_competitor_admin_fields.py` 为同行主档增加公司官网 URL，为同行逐笔成交增加可空数量与供应商名称；数量使用 `NUMERIC(14,3)` 且填写时必须为正，项目总价继续作为独立权威金额。原有产品名称、规格型号、产品图片和产品单价同步接入管理员完整 CRUD，真实情报缺失时仍可留空。
- `20260827_0027_unite_sales_project_fields.py` 为优纳特成交项目增加可空产品单价、数量、供应商、产品规格及订单省市；填写后的单价与数量必须为正，历史订单无需回填且合同总额继续作为实际销售权威口径。
- `20260827_0028_order_product_items.py` 为优纳特与同行订单分别新增 `sales_project_product`、`competitor_deal_product` 多产品明细表，并把每笔历史订单转换为第一条产品。订单主表继续保存项目总价、供应商、签约时间、销售归属和来源等统一字段；兼容列暂时保留并镜像第一条产品，便于前后端滚动升级。
- `20260827_0029_order_product_brand.py` 为优纳特与同行的每条产品明细增加可空品牌字段；历史产品不做不可靠推断，管理员可在两类订单的新增和修改表单中逐条补录，统一订单详情及公开地图订单明细同步展示。
- `20260827_0030_salesperson_coverage_scope.py` 把销售覆盖从仅城市升级为市、省、大区、全国四级范围；历史城市记录自动回填为市级，省市和高德编码仅在对应层级保留。降级遇到非市级记录会主动停止，避免静默丢失新业务数据。
- `20260827_0031_authorized_user_coverage.py` 建立区域权限基础：账号身份统一为“普通用户 / 超级管理员”，仅 `admin_syt` 固定为不可修改、不可删除的超级管理员，并新增账号四级覆盖范围表。现有其他账号迁移为普通用户；账号可拥有多个市、省或大区范围，全国范围必须单独使用。数据后台现已向全部登录账号开放，业务清单的查询与写入由服务端按覆盖范围继续收口。
- `20260827_0032_admin_user_salesperson.py` 为授权账号增加可空的销售人员关联及外键索引，并为现有区域演示账号按确定性规则关联销售档案。区域账号后续新增或修改时必须关联一名销售人员；全国账号和超级管理员无需关联即可查看全部销售 Pin。
- 首页业务热力图以“成交金额 / 采购意向”分段按钮切换互斥指标，不再用斜线把意向叠加在成交底色上。成交模式使用固定橙色金额档位，保留卖方和年份筛选；年份由 API 返回，切换后省级颜色与点击订单明细同时按 `signed_at` 的自然年过滤。采购意向模式仅显示优纳特当前有效、尚未成交的意向，使用按当前最大预计金额归一化的五档绿色，图例同步展示每一档的动态万元区间；左下角以“当前有效意向”明确其快照口径，不伪造历史年度意向。
- SQLAlchemy 引擎显式限制连接池大小、溢出、等待、回收、连接超时和 PostgreSQL `statement_timeout`，并保留 `pool_pre_ping`。公开单位查询与销售汇总统一排除归档记录，管理员可通过专用筛选查看和恢复归档。
- 站点认证采用 HTTP-only 会话 Cookie + 严格 SameSite CSRF Cookie/请求头双提交校验；登录校验浏览器 Origin，连续失败触发数据库账户锁，网关另按 IP 限速。超级管理员可维护普通用户的账号状态、多个市/省/大区/全国覆盖范围及区域账号关联的销售人员；`admin_syt` 的身份和全国范围受服务端保护。更新范围时先删除并刷新旧子记录，再插入完整新集合，避免保留原范围并新增范围时误触唯一约束。主站权限按页面划分：典型案例继续全国开放，优纳特成交热力全国开放；销售覆盖与人效中，区域普通账号只获得关联销售人员的 Pin，全国账号与超级管理员获得全部销售 Pin；全国单位地图、单位公开列表和筛选项按账号市省范围过滤；客户关系图只显示范围内总部及范围内节点；同行先以任一范围内据点或成交客户完成整家公司准入，再把成交热力、订单和对比意向限制在账号负责区域。数据洞察默认仅聚合账号负责市省，切换大区视角后，只要负责一个省市即可展开其所属完整大区。数据后台入口对全部登录账号可见；普通用户的单位、集团、同行、成交订单和案例项目候选写入按覆盖范围交集校验，跨省记录命中任一负责地区即可整体维护。销售数据库及其通用资源接口仅全国账号和超级管理员可读取、添加、修改或删除，区域普通账号不显示销售页签且服务端直接返回 403。缺少可靠省市归属的常驻点和渠道主档仅超级管理员可写；授权账号页及其 API 始终仅超级管理员可用。生产配置在 `APP_ENVIRONMENT=production` 时强制要求 `ADMIN_COOKIE_SECURE=true`。
- `/api/v1/health/live` 只检查 API 进程，`/api/v1/health/ready` 用短查询检查 PostgreSQL 并 PING Redis；Compose 只在 Redis 与 API 就绪后启动依赖服务。Nginx 为 API 设置连接/读写超时、请求 ID、登录限流、CSP、禁止嵌入与类型嗅探等入口安全边界；CSP 仅为高德 JSAPI 2.0 插件/WebGL 模块保留其必需的 `unsafe-eval` 兼容项，其余脚本仍受同源和高德域名白名单限制。
- 单位后台保留当前页选择、批量工具栏和二次确认；不提供保存筛选视图或最近访问记录，也不读写对应浏览器本地存储。首页无行为的通知与头像按钮已移除；桌面地图入口保持右侧纵向导航，地图画布顶端对齐，热力图等地图继续使用各自原有的左侧筛选与图例布局。
- `frontend/Dockerfile` 默认基于官方 `node:22-slim`；受控断网维护时可通过 `NODE_IMAGE` 构建参数复用已审计的本地 Web 镜像，不改变常规在线构建来源。Windows 宿主机额外以 webpack 路径完成等价生产构建验证；若镜像仓库不可达，`Dockerfile.offline` 覆盖已验证的 standalone JS 依赖与 server/static/public 产物，并由 `.dockerignore` 排除 Windows 原生二进制、保留基础镜像中的 Linux Sharp。
- 首页默认全国单位地图在 900px 以下统一解除网格子项的固有最小宽度，并把当前五个可见地图入口的横向滚动限制在切换轨道内部；筛选栏、地图画布和标签轨道不再把移动端工作区撑出卡片。同行市场版图入口暂时关闭，但组件、视图分支和数据链路均保留。桌面端选择省、市、区后按当前点位经纬度边界自动计算中心和层级，左右各保留 96px，底部为 Legend 保留 112px，并把单点或密集区域最大缩放限制为 12 级。`home-organization-map.spec.ts` 锁定响应式边界和地区视野算法。
- 同行全国总览继续只调用 `GET /api/v1/public/competitors`，但响应新增轻量 `strength_regions`；服务端用固定次数的 `selectinload` 批量加载计算原料，不再由浏览器对每个同行并发请求包含客户、交易和关联单位的完整详情。点击单个据点或区域时仍按需读取完整详情，现有地图功能和数据口径不变。
- `frontend/src/lib/html.ts` 是所有高德自定义 Marker/Text 的共享 HTML 转义入口；单位、集团、同行和销售地图不再各自维护重复实现。`frontend/src/lib/api.ts` 只在存在请求体且调用方未显式设置时补 JSON Content-Type，GET 请求保持最小请求头；管理员会话恢复请求支持卸载取消。
- Next.js 开发回退代理为高德上游请求设置 10 秒终止信号，Nginx 正式代理设置 5 秒连接和 15 秒读取超时；外部地图服务异常不再无限占用请求槽位。
- GSAP 首页切换动画保留现有 `useGSAP` 作用域、`matchMedia` 减少动态偏好、更新回滚和卸载清理。审查未发现动画泄漏或强制布局热点，因此不做无收益重写。
- 管理员 Cookie 由 `ADMIN_COOKIE_SECURE` 控制：本地 HTTP 默认 `false`，HTTPS 演示或生产入口必须设为 `true`。成功登录会在同一事务回收该管理员的过期会话，避免长期运行累积历史行。
- 登录入口接受显式 `CORS_ORIGINS` 白名单或 Origin 与请求 Host 完全一致的浏览器请求，兼容受信反向代理的动态域名；跨站 Origin 仍在校验凭据前返回 403。
- `20260819_0016` 为 `opportunity.estimated_amount` 和 `sales_project.contract_amount` 增加数据库非负 CHECK，迁移前先拒绝历史负数脏数据；`20260819_0017` 删除早期遗留、与 GeoAlchemy `idx_organization_site_location` 覆盖相同列的重复空间索引。
- `backend/alembic/env.py` 使用 GeoAlchemy 官方改写器，并从 PostgreSQL 系统目录识别扩展拥有的关系。`alembic check` 因此忽略 PostGIS/TIGER 自带对象，但仍审查未知业务表、字段、约束和索引；扩展归属查询结束后显式提交只读事务，避免包住并回滚后续迁移。
- 根 `.gitignore` 与 `backend/.dockerignore` 均排除 `.pytest-tmp*`，防止 Windows 上权限收紧的 pytest 临时目录污染工作区状态或阻断 Docker BuildKit 读取构建上下文。

## 2026-08 一省一案典型案例数据库

- `typical_case` 保存省级案例展示快照，并可通过 `sales_project_id` 关联真实成交项目；项目删除时使用 `ON DELETE SET NULL` 保留已策展故事。合同金额和签约日期始终从 `sales_project` 实时读取，预计金额、联系人、跟进负责人和内部备注不进入公开案例 DTO。
- 图片和成果指标是案例内部的小型有序集合，使用受 Pydantic 严格校验的 JSONB 保存；图片路径只允许 `/cases/` 下的 WebP、PNG、JPEG 或 AVIF，发布案例必须包含且仅包含一个封面，最多 5 张图片和 4 个指标。
- `20260818_0014` 迁移创建案例表，并关联公司1至公司6的六笔虚构成交项目，写入江苏、吉林、广东、重庆、浙江、广西六条明确标记为演示的去敏案例。`20260819_0015` 将省份与关联成交项目唯一性扩展到草稿和已发布全状态，保证每省最多一个案例、每笔成交项目最多被一个案例关联；全站仍最多一个推荐案例。
- 授权主站的 `GET /api/v1/public/typical-cases` 一次返回大陆 31 个省级区域及上线/筹备统计，`GET /api/v1/public/typical-cases/{id}` 只读取已发布详情，合同金额还需案例显式允许展示。受管理员会话保护的 `/api/v1/admin-typical-cases` 固定返回 31 个轻量省份槽位和“未配置/草稿/已上线”状态，不分页也不返回故事正文；点击后再读取完整详情，成交项目只搜索当前省份，所有写入均记录审计日志。
- `POST /api/v1/admin-typical-cases/images` 校验管理员会话、文件类型、8MB 大小与解码结果，去除元数据并用 Pillow 统一转换为 UUID 命名的 WebP。Compose 将 `frontend/public/cases/` 同时挂载为 API 可写目录和 Web 只读静态目录，因此新图片可立即被前后台读取；删除数据库案例不会自动删除图片文件，避免误删仍被引用的素材。
- 首页右侧当前第五个可见入口“典型案例地图”按需加载 `home-typical-case-map.tsx`。组件先读取 31 个大陆省级区域和发布统计，再按已发布案例 ID 请求完整复盘；地图路径、原生省份下拉、Enter/Space 键盘操作共用同一选择状态，当前省份路径固定在 SVG 最上层，避免焦点框被邻省遮挡。已上线、筹备中、图片失败、列表/详情加载失败及空数据各有独立反馈，移动端改为地图在上、案例在下的单列阅读。

## 2026-08 销售人员覆盖与活动数据库

- `salesperson` 保存稳定员工编号、展示姓名、地图颜色和启用状态；演示姓名采用“张1、王3、冯7”等固定虚构值，不在请求时随机变化。人员业务数据通过 UUID 关联，姓名修改不会改写历史归属。
- `salesperson_coverage_city` 保留历史物理表名，当前通过 `scope_level`、`scope_name` 保存市、省、大区或全国范围；市级继续保存省、市和高德六位 `adcode`，省级只保存省份，大区与全国不伪造城市编码。唯一约束禁止同一销售重复添加同级同名范围，同时允许多名销售覆盖同一区域。
- `sales_activity` 保存拜访、演示、市场活动三类活动的责任人、发生时间、城市及可选关联单位。`(salesperson_id, occurred_at)` 与 `(amap_adcode, occurred_at)` 复合索引分别支持人员时间窗口和城市活动密度聚合。
- `opportunity.salesperson_id` 保存储备项目责任人，`sales_project.salesperson_id` 保存实际成交归属；两列均使用 `ON DELETE SET NULL`，防止人员变更级联删除商机或成交。`sales_project` 保存供应商、订单省市和合同总额，`sales_project_product` 按顺序保存多条产品名称、规格、单价、数量和产品总价；预计金额不会计入实际成交额。
- `20260813_0010` 迁移创建上述三表和销售活动枚举，写入 6 名、30 条城市覆盖、108 条跨 1/3/6/12 月时间窗口的虚构活动及 12 条虚构商机，并把现有 9 笔虚构成交项目分配给销售人员。杭州由两名销售共同覆盖，专门用于验证冗余覆盖；所有演示备注均明确标记为虚构数据。
- 七个固定大区定义为：浙江区（浙江、江西）、东区（江苏、安徽、上海、山东、河南）、北区（黑龙江、辽宁、吉林、内蒙古、河北、山西、天津、北京）、西区（云南、贵州、湖南、湖北、四川、重庆）、南区（广西、广东、福建、海南）、西北（陕西、甘肃、宁夏、青海）、其他（新疆、西藏）。后端集中校验该映射，公开 DTO 同时返回大区或全国展开后的省份列表，便于后续地图和统计复用。
- 授权主站的 `GET /api/v1/public/salespeople/coverage?months=3` 按账号权限返回在职销售的地图 Pin 坐标、分级负责范围和人效汇总：区域普通账号仅返回 `admin_user.salesperson_id` 关联的一名销售，全国账号与超级管理员返回全部销售；历史区域账号未关联时安全返回空列表。`months` 只允许 1、3、6、12，活动按滚动自然月过滤，成交金额按该销售累计已成交项目汇总，储备金额按当前未关闭失单商机汇总。第五地图不查询行政区边界、不绘制 Polygon 或 Circle，只用一个 Marker 表示一名销售；市、省、大区、全国标签继续在单人详情和双人对比中以文字展示。

## 2026-08 客户集团关系数据库

- `customer_group` 与现有 `organization` 完全隔离，保存集团名称和稳定十六进制颜色；不保存可从单位树计算的分支数、成交数、覆盖省市或销售汇总，避免重复口径。
- `customer_group_unit` 以 `parent_id` 自关联表达“总部、一级分支、二级分支”及后续任意层级。数据库限制每个集团最多一个总部，总部必须是根节点，普通单位必须有父级；触发器拒绝跨集团父级和循环父子关系。
- 单位 `longitude/latitude` 保存 AMap 展示使用的 GCJ-02 坐标，`location` 保存转换后的 WGS84（SRID 4326）PostGIS 点位。`actual_sales_amount` 与 `estimated_opportunity_amount` 均使用 `NUMERIC(14,2)` 且独立保存，预计商机金额不计入实际成交额。
- `20260812_0005` 迁移写入 3 个集团、12 个虚构演示单位，名称采用“集团1”“集团1一级分支1”等明确演示格式；6 个单位含虚构成交额，其余单位覆盖活跃、失单和无商机状态。首页公开接口和客户关系网络已经接入，集团主档及单位树现可在统一管理员数据后台维护。
- 授权主站的 `GET /api/v1/public/customer-groups` 只返回集团主档与总部节点；用户点击总部后，`GET /api/v1/public/customer-groups/{group_id}` 才加载该集团完整单位树。分支数、分支成交数、活跃商机数、实际成交总额及覆盖省市均由服务层从单位记录实时计算，预计商机金额不进入实际成交总额。

## 2026-08 同行市场版图与正式单位关联

- `competitor`、`competitor_site`、`competitor_customer`、`competitor_deal`、`competitor_deal_product` 与 `competitor_strength_region` 构成独立同行数据域。订单主表保存项目总价、中标时间、供应商和来源；产品表按顺序保存多条名称、规格、图片、单价、数量及产品总价，允许真实情报字段缺失。当前公开地图的强中弱区域从据点与订单主表实时推导；同行数据不改变本公司客户状态，也不计入本公司实际销售额。
- `competitor_customer_organization_link` 是同行成交单位与现有 `organization` 的多对多关系表；匹配状态、方式、置信度、确认人和确认时间独立保存。一个正式单位可关联多个同行，同一个同行成交单位也可保留待确认或拒绝的匹配历史。公开单位 DTO 只动态投影已确认关联的同行名称、成交级别、交易额、来源与情报置信度；不把关联信息塞入 `organization.attributes`。
- `20260812_0006` 迁移创建同行数据域并写入 10 个虚构同行、17 个据点、60 个虚构同行成交单位、60 笔虚构交易及初版静态区域；`20260812_0007` 将据点、成交单位和交易额重排为区域集中型画像，并删除随机区域演示行。同行3、同行7等可只形成一个强区，同行6稳定形成强/中/弱三档，所有低于证据门槛的省份保持空白。迁移同时保留“公司1/公司2/公司3”三个虚构 `organization` 关联，其余 57 条保持未关联。
- `20260813_0008` 把“公司1”至“公司6”维护为正式单位库中的六家纯虚构已成交客户，华北、华东、华南各两家；九笔虚构 `sales_project` 分别记录合同金额、签约日期和项目说明。地图实际成交额只汇总 `sales_project.contract_amount`，预计商机金额不参与计算。
- `20260813_0009` 将六家已成交演示客户从省会及一线城市分散到盐城、吉林、湛江、重庆、金华和柳州，同步更新 GCJ-02 展示坐标与 WGS84 PostGIS 点位，用于观察同行强势区内外的差异化客户分布。
- 授权主站的 `GET /api/v1/public/competitors` 只返回每个同行的主要据点；点击或筛选后，`GET /api/v1/public/competitors/{competitor_id}` 才返回该同行据点、成交单位、交易、动态区域及实时汇总。省级区域评分为“据点权重占比 20% + 成交单位数量占比 35% + 交易额占比 45%”，总部/分部/服务点的据点权重分别为 3/2/1；强、中、弱阈值依次为 45%、25%、12%，低于 12% 不展示。来源类型、来源说明、可空来源 URL 与情报置信度仍保留为后续录入字段；区域强度和情报置信度是两个独立维度。
- 授权主站的 `GET /api/v1/public/organizations/won-customers` 在数据库层筛选 `customer_status=已成交客户`、主地点且已定位的正式单位，只返回地图点位、安全单位字段及逐笔实际成交项目；联系人、内部备注、跟进负责人和预计商机均不进入该 DTO。
- 同行地图当前以 GCJ-02 经纬度直接供 AMap 展示，不执行空间数据库查询；未来如增加 PostGIS 半径、相交或聚合查询，必须像 `organization_site` 一样同步写入 WGS84、SRID 4326 的 `location`，禁止直接拿 GCJ-02 做空间计算。

## 2026-08 全栈保守优化与安全加固

- 单位筛选分页不再连接地点表后直接 `offset/limit`，统一改为关联 `EXISTS` 条件；多地点单位只占一个分页槽位，省、市、区及编码状态仍要求命中同一地点。公开目录使用独立轻量查询，只预加载公开地点并在 SQL 中聚合证据数量，不再读取联系人、商机、成交项目或证据正文。
- 管理端手工新增/修改地点与自动地理编码复用同一 GCJ-02→WGS84 转换入口：`longitude/latitude` 继续供 AMap 显示，PostGIS `location` 始终按 SRID 4326 写入。单位改名也执行排除自身后的标准化同名检查。
- Excel 导出对所有字符串统一防公式注入处理；以 `= + - @` 或控制字符开头的内容以纯文本保存。离线测试覆盖导出公式、管理员坐标转换、客户集团模型、公开集团 API、聚合管理档案与单位并发完整性，后端当前共 149 条测试。
- 公开单位库和管理员工作台对搜索做 250ms 防抖，在筛选/页容量事件内原子回到第一页，并取消过期请求；列表、层级选项和地图点位使用独立错误边界，旧响应不会覆盖新筛选。FastAPI 的 Pydantic 422 数组会被格式化为字段级可读消息。
- `AdminOrganizationMap` 使用明确的最小 AMap 类型边界；Loader 失败不会永久缓存，SDK 加载具备两次有限超时，卸载会取消异步初始化。MarkerCluster 回调通过最新数据 ref 回查点位并兼容 `extData`，筛选变化后不会继续打开旧单位。
- 首页移除约 500 行已无入口的旧 SVG 项目地图/关系网实现；保留当前 AMap 点位图、省级热力图和数据洞察。GSAP 入场仅动画 `transform/opacity`，移除大面积 `filter: blur()` 重绘，并继续尊重 `prefers-reduced-motion`。
- 前端生产依赖移除 13 个源码零引用包；Next.js / `eslint-config-next` 升至 16.3.0，并将审计收敛为 0 个已知漏洞。生产镜像以 `npm ci` 复现依赖，使用 Next standalone 输出且以非 root `node` 用户运行，不再复制完整开发依赖。
- 管理员详情请求通过 `useLatestRequest` 统一执行 latest-request-wins：单位、客户集团和销售人员连续点击时会取消旧请求，迟到响应不能打开或继续操作错误档案。单位列表与地图的 100/10 条页容量切换按当前首条偏移换算新页码，并按总量钳制边界；销售活动的单位候选只在搜索框聚焦后按需加载，不再随历史活动数量产生 N 个并发请求；同行据点、成交单位、交易或关联变化会使公开强势区域快照失效并重新计算。
- `20260818_0013` 在实表确认 718 条标准化名称无重复、每个单位至多一个主地点后，增加名称唯一索引和“每单位仅一个主地点”的部分唯一索引；应用层前置友好校验继续保留，数据库负责封住并发写入窗口。名称、统一社会信用代码和主地点冲突统一回滚并返回中文 409，不暴露 SQL。五类单位子记录外键索引与稳定分页复合索引继续支撑两万级单位关系加载，无需改成 JSON 或替换规范化关系结构。
- 清理前端目录中 5 个零字节浏览器调试残留，并把 Playwright `test-results/` 加入忽略规则。管理员页 Impeccable 排版/布局扫描未发现反模式；后台只保留短时颜色与背景过渡，全局继续尊重 `prefers-reduced-motion`，不额外引入动画依赖。
- 单位筛选补充统一 `focus-visible` 焦点环；详情抽屉改用原生模态 `dialog`，获得焦点约束、Esc 关闭、背景隔离与触发点恢复。整行详情入口保持原视觉，但可访问结构改为“单位记录列表 + 信息完整的详情按钮”，不再把 `cell` 错误嵌入按钮；成功、失败和删除后果分别通过 status、alert 与 `aria-describedby` 对辅助技术播报。

## 2026-08 行业单位地图与单位数据库数据接入

- `localhost:3100/` 首先只挂载与后台入口同风格的登录弹窗；授权成功后才挂载主站业务组件并读取 `/api/v1/public/*`。全部登录账号都看到“数据后台”入口；退出会撤销服务端会话并立即卸载业务视图。公开 DTO 仍只包含页面实际使用的去敏字段。
- `localhost:3100/admin/organizations` 与主站同属一个 Next.js 部署；全部登录账号均可进入数据后台。普通用户看到“全国目标单位、成交订单、销售与渠道、客户集团、同行、销售、典型案例”七个业务页签，列表、筛选项、详情和写操作由后端按账号覆盖范围过滤；跨省单位、集团或同行命中任一负责地区即可整体维护。仅超级管理员额外看到并可调用“授权账号”，可新增普通用户、编辑启用状态和多个市/省/大区范围，或配置单独的全国范围，并二次确认删除；`admin_syt` 始终只读。全国目标单位默认不挂载地图且列表占满内容区；“显示地图/关闭地图”在列表标题栏切换双栏视图，地图展开时按当前首条记录偏移把列表容量换算为每页 10 条，关闭时换回用户选择的容量，因此不会跳过当前批次或产生越界空页；“仅已核验地图 pin”只在地图标题栏出现且不改变列表或导出条件。地图展开时隐藏列表操作列，五个信息列自适应填满列表卡且不产生横向滚动；地图关闭后恢复修改与删除操作。
- 销售与渠道复用 `/api/v1/admin-data/{resource}` 受保护 CRUD；同行详情继续复用该入口，并由嵌套产品编辑器在单一事务同步一笔订单的多条产品。`/api/v1/admin-deals` 统一展示优纳特与同行订单，支持按订单归属、供应商、同行公司、产品名称和签约年份组合筛选；成交订单页可直接新增两类订单，并在金额下修改或二次确认删除。同行写入复用资源白名单 CRUD，优纳特订单级 CRUD 直接维护既有 `sales_project` 与多产品关系，和全国单位页、同行页共享同一数据库记录、外键完整性和审计日志。
- 客户集团页只保留集团主列表，不再把“集团主档”和“总部与分支”拆为两个分类。`GET /api/v1/admin-customer-groups` 先稳定分页集团，再以固定两次批量查询汇总总部、分支数、成交单位数、活跃商机数、实际成交额与预计商机额，不逐集团加载单位树。点击“修改”后才按需读取完整档案；新增和修改通过草稿键表达同批次新节点父子关系，在单一事务内同步主档和任意层级单位树，同时维护 GCJ-02 展示坐标与 WGS84 PostGIS 点。数据库仍保留规范化的集团主表和单位自关联表，未物理合表、未迁移现有数据；总部记录可修改全部业务字段但不能被另一条记录替换，避免唯一总部约束出现瞬时冲突。删除继续二次确认并级联完整单位树。
- 销售页只保留销售人员主列表，不再展示覆盖和活动两个分类列表。`GET /api/v1/admin-salespeople` 用固定数量的批量查询返回每人最多 10 个带层级标签的覆盖范围、范围总数、全部实际成交金额、近 3 个自然月的拜访/演示/市场活动数量及状态，避免逐人补查。点击“修改”后按需请求完整档案，在同一编辑层维护人员主档、市/省/大区/全国覆盖与销售活动；切换层级会清空失效字段，全国不能与其他范围并存。新增和修改均由聚合服务在单一事务同步两个子集合，任一约束失败会整笔回滚。
- 管理员单位列表保留详情抽屉和独立“修改/删除”操作列；新增与修改对话框按“基本信息与合作进展、联系人、成交项目、商机、主地点与定位”分区。每个成交项目维护项目名、项目总价、供应商、订单省市、日期、商机、负责销售和详情，并可增删多条产品；每条产品独立维护名称、规格、单价、数量和产品总价。订单、产品及单位其他关联记录在同一事务保存，新增订单默认继承单位主地点省市。
- `organization` 通过 `20260810_0004` 增加最近跟进时间、最近跟进内容、跟进负责人、合作意向和合作等级。授权主站只读取最近跟进时间/内容及合作意向/等级；跟进负责人、联系人电话邮箱、管理员备注等受保护字段不进入公开 DTO。
- `backend/app/routers/organizations.py` 的 `/organizations/filters` 按账号范围及已选省、市返回 `provinces`、`cities`、`districts`；管理员账号自动展开为全国。公开 `map-points` 在可信主地点查询中直接应用市省条件，公开单位列表以任一匹配地点准入并在响应中移除范围外地点；点位继续返回主地址、有效商机数、最深推进阶段和预计金额合计，不返回联系人、跟进动作或内部备注。旧 `/public/organizations/province-summaries` 单位数量热力接口已删除，避免与成交金额口径并存。
- `frontend/next.config.ts` 在本地开发把同源 `/api/v1/*` 代理到 `BACKEND_API_ORIGIN`（默认 `http://localhost:8000`）；容器部署仍由 Nginx 网关代理，因此管理员 cookie 不需要跨域传递。
- `frontend/src/app/page.tsx` 的主导航保持“全国单位地图”，首页默认显示原 AMap 点位聚合地图；右侧当前只开放“全国单位地图”“全国成交热力地图”“客户关系网络”“销售覆盖与人效”和“典型案例地图”五个标签。同行市场版图通过文件内布尔开关隐藏入口，动态组件、`competitors` 视图类型、渲染分支和页脚说明均未删除，重新开启开关即可恢复。点位视图保留省、市、区、单位类型和客户状态筛选；关系地图与案例地图均先读取轻量列表，再按点击对象加载详情。
- `backend/app/routers/deal_heatmap.py`、`backend/app/services/deal_heatmap.py` 与 `backend/app/heatmap_schemas.py` 组成成交热力读取链路。优纳特实际金额固定来自 `sales_project.contract_amount` 并对所有登录账号全国可见；同行项目总价来自 `competitor_deal.amount`，卖方先按任一据点或成交客户判断整家公司准入，随后可用年份、省级汇总、逐笔订单和优纳特意向对比全部限制在账号负责市省，范围外省份详情直接返回 403。订单详情返回 `products[]` 多产品明细，并附成交单位省市、供应商、情报来源与备注。预计金额不进入实际成交汇总。
- `20260825_0023_data_insights_demo_data.py` 只补成交热力现有数据缺失的 13 个省级区域：新增 26 家明确标记的虚构成交单位、91 个分布于 2024—2026 年的成交项目，以及 26 个当前商机和 26 个已关联成交的历史商机。与既有虚构数据合并后，31 个大陆省级区域均有实际成交，2024 年第一季度至 2026 年第三季度每季度至少 10 个项目，足以验证全国数据洞察的省市贡献、Top 10、同比、环比、趋势和商机排除口径；所有单位名称、地址、备注及 JSON 属性均保留“演示/虚构”标记，不代表真实客户或业绩。
- `frontend/src/components/home-organization-heatmap.tsx` 默认展示优纳特成交合同总金额，使用 1–150 万、150–250 万、250–300 万、300–400 万、400 万以上五个固定档位，使重排后的全国演示数据在各色阶间接近均匀，同时保持公司切换前后可横向比较。公司下拉直接读取数据库启用同行；优纳特采购意向以默认关闭的青绿斜线独立叠加，意向独有省份也可点击。金额与意向图例固定在地图框左上角，缩放控件保持左下角。省份详情按成交订单与意向分区：优纳特订单突出合同总金额，同行订单在原有产品与来源字段外继续展示成交单位省市、同行官网和逐笔备注，长字段复用整行信息网格。销售常驻点、经销商、代理商及合作伙伴的原有开关、筛选和图层行为保持不变。
- `sales_office_location` 独立保存销售常驻点名称、城市、地址、GCJ-02 坐标、覆盖半径与启用状态；`20260810_0002` 迁移写入杭州、北京、天津、沈阳、成都、广州、武汉、南京、西安九条明确标记的演示数据。主站只读取启用点，受管理员会话保护的查询与修改 API 可维护全部字段。
- `channel_partner_location` 保存渠道名称、类型、地址、可空业务坐标、演示定位坐标、可空授权区域、覆盖半径、可空产品线、合作等级、合同、备注与启用状态；`20260810_0003` 迁移写入三类各 6 条、合计 18 条省会城市演示数据。业务经纬度保持空值，地图暂用独立演示坐标；以后录入业务经纬度后公开地图 API 自动优先使用业务坐标。授权主站接口只返回绘图必要字段，不公开合同、备注、产品线或业务坐标。
- `frontend/src/components/home-organization-database.tsx` 在“类型 / 行业”右侧以主地点展示“省市区”，并新增默认空白的“最近跟进”列；七列紧凑栅格排列商业与联系信息。单位存在已确认同行关联时，名称下方显示“同行已签约”和同行、成交级别、交易额摘要，数据来自关系表而不是单位状态字段。
- `backend/tests/test_api_contracts.py` 覆盖筛选接口的成功层级结果、超长地点参数校验与未认证访问；`backend/tests/test_deal_heatmap_api.py` 覆盖成交热力卖方、汇总、详情的成功路径，以及未授权和超长卖方参数校验。

## 2026-08 已核验 985/211 高校与主校区编码

- `backend/app/cli/import_verified_985_211_universities.py` 是 39 所 985 与 60 所仅 211 高校的可重复执行导入入口：以教育部官方名单为底表，保留两轮筛选结论与官网复核入口；已有档案只会记录为重复候选，不会被覆盖。
- `backend/app/services/geocoding.py` 对“城市+单位名”的历史粗地址新增严格同名主 POI 策略：优先同名或名称明确标注“主校区”的高德 POI；仅在不存在前者时回退到同名且非附属机构的学校校区，补写地址和行政区后再创建 GCJ-02/ WGS84 点位。该策略用于 985/211 主校区、全部体育院校及四川大学低置信度点位。
- 地理编码外部请求单次最多等待五秒；超时记录保持“待编码”，由后续批次或人工补地址处理，绝不以省市中心坐标替代。
- 暂缓的外部请求会更新尝试时间并在待编码队列中后移，避免单个高德慢响应阻塞后续学校；已成功或失败的单条记录仍各自独立提交。
- `backend/tests/test_verified_985_211_universities.py` 与 `backend/tests/test_geocoding.py` 分别覆盖 39+60 名单边界和粗地址 POI 约束；两者均为离线测试，不请求高德或读取真实 Key。

本文件描述当前仓库真实存在的项目结构；规划中的后端与数据库不会被当作已实现功能。项目根目录固定为 `D:\桌面\优纳特销售网站`。

```text
优纳特销售网站/
├─ frontend/                 Next.js 仪表盘、高德地图测试页与单位审核后台
├─ backend/                  FastAPI、Alembic、PostGIS ORM 与单位审核 API
├─ infra/nginx/              同源网关与高德安全服务代理模板
├─ docker-compose.yml        本地全栈容器编排（PostGIS、Redis、API、Web、Nginx）
├─ docs/                     项目说明与需求文档
├─ .venv/                    本地 Python 虚拟环境，禁止提交
├─ AGENTS.md                 面向协作开发者与 AI 的全局工作规范
├─ PRODUCT.md                产品目标、用户与视觉原则
└─ .gitignore                根目录忽略规则（含各层级 ui-check 浏览器截图与本地日志）
```

## 前端：`frontend/`

Next.js 16.3 App Router 应用。当前仪表盘使用 TypeScript、React、GSAP、`@svg-maps/china` 和 Tailwind/PostCSS；高德地图在首页默认点位视图和管理员按需地图中加载。

| 路径 | 作用 |
| --- | --- |
| `src/app/page.tsx` | 主站会话边界与仪表盘页面；先恢复授权账号会话，成功后才挂载三栏地图、单位数据库和数据洞察，退出后立即卸载业务视图；全部登录账号都显示数据后台入口。 |
| `src/components/access-login-panel.tsx` | 主站和数据后台入口共享的登录弹窗；凭据只提交同源认证 API，可按调用页面选择是否要求超级管理员。 |
| `src/app/layout.tsx` | App Router 根布局；加载全局样式、设置中文语言和页面元数据。 |
| `src/app/globals.css` | 全站设计令牌、仪表盘布局、响应式断点及动效样式。 |
| `src/app/amap-service/[...path]/route.ts` | 高德 REST 服务代理；在服务端附加安全密钥，不向浏览器暴露密钥。 |
| `src/components/home-organization-database.tsx` | 主页“单位数据库”面板；复用公开单位列表与筛选 API，以橙蓝风格展示安全字段。搜索请求防抖且列表/层级选项分离取消，筛选与页容量原子回到第一页。省、市、区联动保持未选省禁用市、未选市禁用区；默认每页 8 条固定视窗，15/25/50/100 条时页面自然展开。 |
  | `src/components/home-data-insights.tsx` | 首页“数据洞察”账号专属聚合界面；默认只读取负责范围内的年度/季度 KPI、全部可见省份、城市订单 Pin、月度趋势、成交单位和商机阶段，不再把全国区域贡献做成 Top 排名。大区视角会把任一负责省市扩展为所属完整大区，并按七个固定大区聚合金额热力；同一大区内省份使用同一服务端聚合色值，仍可按省下钻到城市。全国、省、市请求均携带相同范围模式，支持取消、加载、空数据、错误状态和同口径 Excel 导出。桌面地图与可滚动完整区域清单使用同一 585px 高度；省级下钻继续按 viewBox 与实际画布反算 Pin 屏幕尺寸，并对重叠标签上下错位。 |
| `src/components/home-organization-heatmap.tsx` | 首页省级单位热力组件；集中维护五档阈值和橙红色阶，以下拉面板保留档位多选，并在左下缩放控件上方呈现紧凑五色色阶；销售常驻点保留覆盖圈，三类渠道仅绘制 Pin，四类可选图层共用底图投影，渠道支持独立多选及合作等级筛选。 |
| `src/components/home-group-network-map.tsx` | 首页客户关系网络组件；默认只显示数据库中的集团总部，并在统一的半透明标题浮层中提供数据库驱动的集团下拉框，点击总部仍可按需加载单集团全部节点并以 Marker/Polyline 绘制层级树。总部使用当前集团色的紧凑地图 Pin，“总”字严格按圆形主体而非整个 Pin 外框居中，名称和状态位于 Pin 上方，尖端通过 `bottom-center` 准确落在总部坐标；分支继续使用圆形层级节点。集团概览以当前集团色的纵向单位列表完整显示公司名、总部/分支层级和省市，点击单位进入详情；地图画布使用独立底层堆叠上下文，详情面板使用不透明暖白底，Marker 不会覆盖、透出或截获面板操作。相机仅按全部单位 Marker 平滑适配，并按高德 `[上, 下, 左, 右]` 顺序同时避让左侧标题卡、右侧详情面板及上下边缘，同时额外保留 Pin、名称和状态标签的安全留白；右下角横排缩放控件在详情打开时自动避让面板，关闭、Esc 或选择“全部集团总部”会清理分支与连线并恢复总部。 |
| `src/components/home-competitor-market-map.tsx` | 首页同行市场版图组件；当前主页面入口关闭，但组件、公开 API 和全部交互代码保留。全国总览只绘制主要据点 Pin，不绘制强中弱区域 Circle，也不提供“显示强势区域”按钮。标题卡保留同行筛选和“显示优纳特客户”；优纳特客户首次开启时按需读取正式单位库的已成交客户接口并缓存。单同行的成交单位 Tab 顶部使用原生下拉框选择单位，选项保留单位名、城市和累计总价并默认第一项；下方继续展示单位档案和逐笔项目/产品成交详情，不重复展示单位级来源、置信度和演示说明，逐笔成交来源仍完整保留。单同行视图只绘制据点与成交单位 Pin，并按当前同行点位边界平滑适配；强势等级和评分依据继续在详情面板展示。 |
| `src/components/home-salesperson-coverage-map.tsx` | 首页当前第四个可见地图入口；每名销售只绘制一个高德 `Marker`，不绘制 Circle、Text 或行政区边界。单人详情、人员选择器和双人对比均按市、省、大区、全国标签展示负责范围，不再把范围数量误写为城市数；月份、人效、聚焦避让和独立错误状态保持原有口径。 |
| `src/components/home-typical-case-map.tsx` | 首页当前第五个可见地图入口；使用 `@svg-maps/china` 呈现大陆省级发布状态，原生下拉与键盘路径选择同步，按已发布案例 ID 加载完整去敏复盘。右侧编辑式内容区展示封面、客户/行业、摘要、指标、挑战、方案、成果、服务范围、引语及后端允许公开的合同信息；筹备省份和请求、空数据、图片失败均有独立状态。 |
| `src/components/amap-national-test.tsx` | 高德 JS API 全国省级地图实验组件，包含生命周期清理、失败超时和重试状态；当前未绑定主页导航入口。 |
| `src/app/admin/organizations/page.tsx` | `/admin/organizations` 审核后台入口；组合受认证保护的单位工作台。 |
| `src/components/admin-organization-workspace.tsx` | 统一“数据后台”外壳、权限校验和八页横向导航；区域普通账号隐藏销售及授权账号页签，全国账号可管理销售，超级管理员额外管理授权账号；头部将标题、页面入口与返回/导出/退出操作分区，并在中等视口自动换行以避免遮挡。 |
| `src/components/admin-deal-workspace.tsx` | 成交订单统一展示与操作页；紧凑页头提供添加订单，按优纳特/同行、供应商、同行公司、产品和年份筛选，每笔金额下提供修改、删除，展开区完整显示订单字段、多产品品牌、来源和备注。 |
| `src/components/admin-deal-form-dialog.tsx` | 成交订单页共用的完整增改删对话框；按优纳特或同行切换客户外键和专属字段，复用多产品编辑器，并通过原生模态层完成保存失败反馈和删除二次确认。 |
| `src/components/admin-product-items-editor.tsx` | 优纳特单位表单、同行订单表单与成交订单页共用的多产品编辑器；提供产品草稿/API 转换及名称、品牌、规格、单价、数量、分项总价和可选图片字段。 |
| `src/components/admin-account-workspace.tsx` | 超级管理员账号工作台；新增普通用户，展示并编辑启用状态、市/省/大区/全国覆盖范围及关联销售人员，区域账号强制选择一名销售供人效地图按 Pin 过滤；通过原生模态对话框确认删除，并把固定超级管理员保持为只读。 |
| `src/components/admin-data-workspace.tsx` | 销售与渠道通用数据工作台及同行详情复用的基础表单/删除对话框；按配置渲染分类搜索、稳定分页、重点字段列表、完整字段新增/编辑、二次确认删除，以及按搜索加载的少量外键候选。 |
| `src/components/admin-competitor-workspace.tsx` | 同行专用聚合工作台；以含公司官网的同行主档为唯一列表，进入原位详情后在主档头部把轻量数据摘要横排在同行身份右侧，操作区只保留修改和删除；窄屏再安全换行。详情中按需分页维护据点、成交单位、含产品/数量/供应商/价格的逐笔交易、正式单位关联与人工强势区域；相关子资源变化会使只读公开区域缓存失效并按需重算。 |
| `src/components/admin-customer-group-workspace.tsx` | 客户集团专用聚合工作台；仅分页展示集团摘要，点击后通过 latest-request-wins 按需加载完整档案，并在同一表单内原子维护主档、唯一总部和任意层级分支。 |
| `src/components/admin-sales-workspace.tsx` | 销售人员专用聚合工作台；仅分页展示人员，点击后按需加载完整档案，并在同一表单内原子维护主档、四级覆盖范围和销售活动；层级切换按需显示大区、省份或城市字段，大区同步展示所含省份。 |
| `src/lib/sales-coverage.ts` | 销售覆盖表单的四级选项、七个大区及 31 个省级短名称；把历史“省/市/自治区”全称归一为当前下拉短名，与后端业务口径保持一致。 |
| `src/components/admin-typical-case-workspace.tsx` | 典型案例专用工作台；唯一列表固定展示大陆 31 个省份，已上线/草稿/未配置统计以轻量文字内联在列表标题右侧，不另占强调色卡片，标题区不显示额外解释文案。点击省份后在原位完整详情页维护主档、同省成交项目、故事、最多 4 个指标、最多 5 张图片、客户引语与发布状态；省份和行政区编码锁定，上传、封面、排序、未保存提醒及二次删除确认均在详情内完成。 |
| `src/components/admin-organization-map.tsx` | 高德 JSAPI 点位聚合组件；由单位审核后台和首页复用。以 `AMap.MarkerCluster` 聚合，Marker 回调通过最新 ref/`extData` 读取当前点位并转义名称；首页单个 Pin 可打开带行政区、地址、经纬度与商机汇总的自定义 `InfoWindow`，支持叉号、点击地图关闭和自动避让，聚合点行为不变；首页地区筛选可按点位 Bounds 自适应中心、层级和四向安全区，后台默认视野不受影响；Loader、安全代理和销毁委托给 `src/lib/amap.ts`。 |
| `src/lib/amap.ts` | 高德地图公共生命周期模块；统一动态 Loader、`/_AMapService` 安全代理、有限超时重试、失败脚本清理、调用标识和地图销毁，供点位图与客户关系网络复用。 |
| `src/lib/html.ts` | 高德自定义覆盖物共享 HTML 转义工具；统一保护数据库名称写入 Marker/Text 的文本和双引号属性边界。 |
| `src/lib/api.ts` | FastAPI `/api/v1` JSON、文件下载与 multipart 请求、同源 cookie、取消信号、字符串/422 数组错误格式化、数据洞察、账号角色、授权账号、主站及管理员响应类型与筛选参数工具。 |
| `src/lib/admin-data-config.ts` | 管理员八页导航与通用资源配置；同行成交表单以 `product-list` 字段维护多条产品，典型案例、成交订单和授权账号由专用工作台承载。 |
| `src/hooks/use-latest-request.ts` | 管理端同类详情请求协调器；用 AbortController 集中实现 latest-request-wins 和卸载清理，供单位、客户集团与销售工作台复用。 |
| `tests/admin-data-workspace.spec.ts` | 统一数据后台 Playwright 回归；受控 API 按真实页码切片，验证默认目标单位页、业务页导航、完整 CRUD、区域普通用户看不到销售及授权账号、全国普通用户可管理销售但看不到授权账号、典型案例单列表与按需详情保存、响应式边界、两万条分页容量换算、详情请求乱序、活动候选按需加载和同行区域缓存失效。 |
| `tests/access-auth.spec.ts` | 真实 localhost 访问控制验收；确认匿名浏览器只看到登录弹窗且业务 API 返回 401，环境变量超级管理员可进入主站和带覆盖范围的授权账号页，并锁定主页面当前只有五个地图入口且同行市场版图不可见。 |
| `tests/home-group-network.spec.ts` | 客户关系网络 Playwright 验收；连接本地全栈验证真实集团展开、单位详情、总部文字在圆身内居中、Esc 重置、移动端边界，并通过请求拦截覆盖加载、空数据和错误反馈。 |
| `tests/home-organization-map.spec.ts` | 全国单位地图 Playwright 验收；离线替身锁定窄屏布局边界、单个 Pin 的可关闭信息 Popup，以及桌面省、市、区筛选的点位 Bounds、四向留白、最大缩放与单点定位。 |
| `tests/home-data-insights.spec.ts` | 数据洞察桌面 Playwright 验收；使用真实登录会话和确定性聚合响应验证负责范围/大区视角参数、全部省份清单、省市下钻、城市详情、唯一 Top 10 成交单位和 Excel 下载。 |
| `tests/home-competitor-market.spec.ts` | 同行市场版图 Playwright 验收代码完整保留；主页面入口关闭期间整文件跳过，恢复首页开关后移除文件顶部跳过声明即可重新执行原有详情、覆盖物、异常与移动端回归。 |
| `tests/home-salesperson-coverage.spec.ts` | 销售覆盖地图 Playwright 验收；使用环境变量管理员仅登录一次并向隔离上下文分发会话 Cookie，避免触发登录限流；验证每名销售一个 Pin、四级覆盖标签、月份切换、双人对比、聚焦安全区、移动端边界和地图 SDK 独立错误状态。 |
| `tests/home-typical-case.spec.ts` | 典型案例地图 Playwright 验收；连接真实公开 API 验证默认推荐案例、六个上线省份、图片与完整复盘，并覆盖下拉/键盘切换、筹备状态、列表失败恢复和移动端无横向溢出。 |
| `tests/fake-amap.ts` | 地图浏览器验收的最小离线 AMap 替身；模拟 Marker/Polygon/Polyline/Text、行政区查询和销毁生命周期，验证地图交互但不进入生产代码。 |
| `src/app/favicon.ico` | 浏览器标签页图标。 |
| `public/brand/unite-logo.png` | 顶栏使用的优纳特品牌标识。 |
| `public/cases/*.webp` | 六张由内置图像生成能力制作并压缩的虚构案例封面；无真实客户、品牌、可识别文字或水印，与迁移中的 `/cases/*.webp` 元数据一一对应。 |
| `public/file.svg`、`globe.svg`、`next.svg`、`vercel.svg`、`window.svg` | create-next-app 遗留的通用 SVG 素材；当前页面未引用，可在确认无用后另行清理。 |
| `next.config.ts` | Next.js 配置；把 `/_AMapService/*` 重写到内部代理路由。 |
| `Dockerfile` / `Dockerfile.offline` / `.dockerignore` | Next.js 常规生产镜像使用 `npm ci` 构建 standalone 并以非 root 用户运行；两种镜像都把 standalone 服务监听地址固定为 `0.0.0.0`。离线运行镜像仅在镜像仓库不可达时复用已审计本地镜像，复制前清空基础层的旧 `.next` 以免孤立 chunk 残留，再覆盖宿主机已验证的 standalone JS 依赖与 server/static/public 产物；构建上下文排除 Windows `.node` 文件和 Sharp 平台包，Linux 原生依赖保留在基础镜像中。 |
| `eslint.config.mjs` | ESLint 的 Next.js Core Web Vitals 与 TypeScript 校验配置。 |
| `postcss.config.mjs` | Tailwind CSS 的 PostCSS 插件配置。 |
| `tsconfig.json` | TypeScript 严格模式、模块解析和 `@/*` 源码别名配置。 |
| `next-env.d.ts` | Next.js 自动生成的类型声明；不应手工编辑。 |
| `package.json` / `package-lock.json` | 前端最小生产依赖与锁定版本；零引用的表单、图表、地图和工具包已移除，锁文件由 npm 生成。 |
| `.env.example` | 不含密钥的高德环境变量模板。 |
| `.env.local` | 仅本机使用的高德密钥配置，已被 Git 忽略，禁止提交或记录具体值。 |
| `.gitignore` | 前端目录的依赖、构建产物、环境变量和本地调试文件忽略规则。 |
| `AGENTS.md` | Next.js 版本差异提醒；前端工作还需同时遵守根目录 `AGENTS.md`。 |
| `CLAUDE.md` | 当前为空的工具占位文件，未承载项目逻辑。 |
| `README.md` | create-next-app 初始说明，尚待替换为本项目的启动与部署说明。 |
| `ui-check/` | 本地浏览器检查产生的截图目录，不属于运行时代码或发布素材。 |
| `.next/`、`node_modules/`、`tsconfig.tsbuildinfo`、`debug.log` | 构建、依赖、缓存或调试产物，均不应提交。 |

## 后端：`backend/`

根目录 `.venv` 已具备 FastAPI、SQLAlchemy、Alembic、PostgreSQL 驱动、GeoAlchemy2、OpenPyXL 与 Redis 依赖。后端已实现 SQLAlchemy/PostGIS ORM、版本化 FastAPI API、服务端管理员会话与首版 Alembic 迁移；GeoAlchemy2 保存可靠坐标点，OpenPyXL 和 Redis 为后续官方名单导入与地理编码队列预留。根目录 `.env.example` 是 Docker Compose、PostgreSQL、高德服务端代理与首个管理员账号的无密钥模板；实际 `.env` 仅在本机保存。

| 路径 | 作用 |
| --- | --- |
| `app/config.py` | Pydantic Settings 配置入口；安全构造 PostgreSQL 连接并从根 `.env`/容器环境读取变量，同时约束案例图片目录和上传大小。 |
| `app/database.py` | SQLAlchemy 引擎、会话依赖与 ORM 基类。 |
| `app/models.py` | 核心 PostgreSQL/PostGIS ORM 模型；优纳特与同行订单均以主表关联多产品明细，授权账号以独立关系表保存多个四级覆盖范围并可外键关联一名销售人员，金额继续使用固定精度。 |
| `app/sales_coverage.py` | 销售覆盖业务口径；定义市、省、大区、全国枚举、七个大区省份映射、省份名称归一、分层字段校验和省份展开。 |
| `app/admin_deal_schemas.py` | 统一成交订单后台的筛选选项、分页、完整编辑字段、优纳特订单写入和多产品响应 DTO。 |
| `app/schemas.py` | API 输入/输出验证；包含账号四级覆盖范围的归一/互斥规则，以及客户集团、同行地图、正式单位关联和优纳特已成交客户地图 DTO。公开地图 DTO 只暴露点位、交易摘要与必要单位字段，不返回联系人等敏感信息。 |
| `app/admin_data_schemas.py` | 统一管理员数据后台的专用输入与分页 DTO；为白名单资源及客户集团、同行、销售聚合档案约束全部业务字段、官网 URL、产品情报、数量、金额、坐标和树关系，不接受任意数据库列。 |
| `app/services/auth.py` | 多授权账号密码哈希、服务端会话创建/撤销、普通用户会话、全国账号及超级管理员依赖；会话校验预加载账号范围，并修复 `admin_syt` 的固定身份与全国范围。 |
| `app/services/authorized_users.py` | 超级管理员账号目录服务；哈希新密码、校验销售人员关联、原子替换普通用户状态和四级覆盖范围、翻译重复用户名，并保护固定超级管理员。 |
| `app/services/admin_data.py` | 统一数据后台服务；以显式资源白名单组合模型、schema、搜索列和外键范围，提供稳定分页、CRUD、审计与关联错误翻译，并把普通用户的资源查询、父级关联和增改删限制在账号覆盖范围；销售人员、覆盖范围和活动资源仅全国账号或超级管理员可用，无可靠行政区归属的常驻点和渠道主档仅超级管理员可写。 |
| `app/services/admin_deals.py` | 统一查询优纳特与同行订单，在数据库内应用卖方、供应商、同行、产品和年份条件，再合并排序分页；同时提供优纳特订单级新增、全字段修改、删除、多产品同步、关联校验和审计。 |
| `app/services/admin_competitors.py` | 同行聚合管理服务；分页主档后以固定批量查询汇总据点、成交单位、关联审核、逐笔交易金额和人工强势区域，避免主列表 N+1 查询。 |
| `app/services/admin_customer_groups.py` | 客户集团聚合管理服务；分页批量汇总总部、单位规模、实际成交和商机，预加载单集团完整单位树，并在单一事务新增、同步或删除主档与树节点。 |
| `app/services/admin_salespeople.py` | 销售人员聚合管理服务；分页批量汇总分级覆盖范围、实际成交额和近三个月活动，预加载完整档案子集合，并在单一事务新增/同步/删除覆盖范围与活动。 |
| `app/services/organizations.py` | 单位名称标准化、同名新增/改名提醒、`EXISTS` 地点筛选、稳定复合排序分页、地点点位和完整档案事务服务；数据后台主列表、计数、导出及公开列表和地图查询统一应用账号市省条件，跨省单位以任一地点命中负责范围准入，公开 DTO 再移除范围外关联地点。Excel 导出仅加载实际使用的地点和证据；已成交客户地图实际金额只从成交项目实时汇总。管理员坐标统一从 GCJ-02 转为 WGS84 PostGIS 点。 |
| `app/services/sales_office_locations.py` | 销售常驻点公开启用点查询、管理员全量查询与带审计的字段更新服务。 |
| `app/services/channel_partner_locations.py` | 渠道合作点公开安全地图查询、管理员全量查询与带审计的字段更新服务；业务坐标为空时仅为地图绘制回退到演示定位坐标。 |
| `app/services/customer_groups.py` | 客户集团公开查询与聚合服务；总部列表只查询账号范围内总部，详情保留范围内节点及连接总部所需祖先，并按可见节点实时计算分支、成交、活跃商机、实际金额和覆盖省市。 |
| `app/services/account_access.py` | 账号数据范围服务；把多个市、省、大区和全国授权归一为可复用 SQL 与响应阶段地点判断，支持负责范围与大区展开，并提供单位、集团、优纳特订单、同行整家公司及后台写入的范围准入和统一 403 拒绝。 |
| `app/services/competitors.py` | 同行公开查询与聚合服务；列表和详情先应用账号同行准入条件，命中后再返回该同行全国据点、成交单位、交易和正式单位确认关系，并按 20%/35%/45% 权重实时计算省级竞争评分、强中弱门槛、覆盖中心和成交汇总。 |
| `app/services/salespeople.py` | 销售覆盖公开聚合服务；可按关联销售人员 ID 收窄在职人员，一次查询分级范围并展开大区/全国省份，按滚动自然月统计三类活动，同时独立汇总累计实际成交和当前有效储备项目。 |
| `app/services/typical_cases.py` | 典型案例公开与管理聚合服务；生成 31 省轻量槽位、按需详情、同省成交项目候选、公开字段裁剪以及带审计的原子增删改。 |
| `app/services/typical_case_media.py` | 典型案例图片写入服务；限制类型/大小/像素，使用 Pillow 完整解码、去除元数据并转为 UUID WebP，临时文件成功后才原子替换。 |
| `app/services/organization_exports.py` | 使用 OpenPyXL 生成单位审核 Excel，保留单位、地点、状态和来源证据字段；所有外部/管理员文本写入前防公式注入，不导出受保护联系人字段。 |
| `app/services/insights.py` | 数据洞察数据库聚合服务；所有实际合同、商机、趋势、客户和阶段查询统一应用账号地点条件，全国省份视角仅返回当前年份/季度实际销售额大于零的省份，大区模式按固定七区扩展并聚合热力；省级请求继续生成市级订单点位。 |
| `app/services/insights_exports.py` | 复用页面聚合响应生成经营概览、区域贡献、月度趋势、成交单位和商机阶段五张 Excel 工作表，并防止数据库文本触发公式注入。 |
| `app/services/imports.py` | 批次导入核心服务；保存原始行、可配置类型的官方证据、默认状态并将重复项标记为候选而非覆盖；首次导入或幂等重跑会把同一官方来源中同名且尚未关联的原始行回写到正式档案，不依赖易变化的处理中状态文案；教育部普通高校底表本身不会以校名推测专业后自动创建正式单位。 |
| `app/services/official_sources.py` | 官方公开来源的通用获取服务；提供 `.env` 可配置（默认 4、范围 1–8）的低并发分页、有限重试、中文编码兼容和页序保留，供未来高校、疾控、食药、环保、公安及研究院名单导入复用。 |
| `app/services/geocoding.py` | 高德 Web 服务地址编码；仅将门牌号/兴趣点等可靠结果，或经同名/同校校区与门牌号双重 POI 校验的结果，写为 AMap 的 GCJ-02 pin，并同步写入 WGS84 PostGIS 点位。 |
| `app/cli/import_c9_universities.py` | C9 九所高校的首批真实导入命令；每条使用官方化学/材料相关院系证据，作为筛选功能验证数据。 |
| `app/cli/import_verified_universities_batch_01.py` | 非 C9 高校逐校官网取证首批命令；保存专业依据、完整校址与筛选标签，创建正式单位并回写教育部底表处理状态。 |
| `app/cli/import_verified_double_first_class_universities.py` | 第二轮“双一流”新增建设高校差集导入命令；31 所逐校筛选后保存 25 所合格高校的官网专业证据，并明确保留 6 所艺术/外交类排除结论。 |
| `app/cli/import_verified_provincial_key_universities_batch_01.py` | 省属重点本科首批导入命令；覆盖华北、东北 23 所由官网确认省属重点/省内双一流/高水平建设层次且具备生化环材、医药或检测方向的公办本科，并以低并发队列定位主校区。 |
| `app/cli/import_verified_provincial_key_universities_batch_02.py` | 省属重点本科第 02 批导入命令；覆盖上海、江苏、浙江 24 所由官网确认省级重点/高水平建设层次且具备生化环材、医药、食品农业或检测方向的公办本科，并以严格主校区 POI 队列定位。 |
| `app/cli/import_verified_provincial_key_universities_batch_03.py` | 省属重点本科第 03 批导入命令；按皖闽赣鲁省级重点、高水平或一流学科建设名单筛选 44 所具备生化环材、医药、食品农业或检测方向的公办本科，排除既有和纯财经高校，并以严格主校区 POI 队列定位。 |
| `app/cli/import_verified_provincial_key_universities_batch_04.py` | 省属重点本科第 04 批导入命令；按豫鄂湘粤省级双一流、高水平、冲补强或一流学科建设范围筛选 49 所具备生化环材、医药、食品农业或检测方向的公办本科，按教育部批复使用“湖南理工大学”现名，排除既有、纯文财经艺术、中外合作和非独立分校区，并以严格主校区 POI 队列定位。 |
| `app/cli/import_verified_provincial_key_universities_batch_05.py` | 省属重点本科第 05 批导入命令；按桂琼渝川自治区/省市一流、高水平、四新或贡嘎计划建设证据筛选 37 所具备生化环材、医药、食品农业、安全质量或检测方向的公办本科，使用教育部底表当前校名，排除既有、纯文财经艺术、民办及证据尚不完整院校，并以严格主校区 POI 队列定位。 |
| `app/cli/import_verified_provincial_key_universities_batch_06.py` | 省属重点本科第 06 批导入命令；补齐四川困难证据批 8 所具备贡嘎/省级一流建设身份及生化环材、食品农业或检测方向的公办普通本科，并固化艺术财经、职业本科、公安专批及“有相关专业但证据不足”暂缓项的逐校原因。 |
| `app/cli/import_verified_provincial_key_universities_batch_07.py` | 省属重点本科第 07 批导入命令；覆盖贵州、云南、西藏 17 所具备省属重点/一流建设身份及生化环材、医药、农林食品或检测方向的公办普通本科，区分既有、财经艺术、公安、职业、民办、证据不足和跨省校址项，并以严格主校区 POI 队列定位。 |
| `app/cli/import_verified_provincial_key_universities_batch_08.py` | 省属重点本科第 08 批导入命令；覆盖陕西、甘肃、青海、宁夏、新疆 27 所具备省级重点/一流建设身份及生化环材、医药、农林食品或检测方向的公办普通本科；西藏民族大学按实际主校区归入陕西，并使用教育部 2026 底表的具体来源页回写追溯状态。 |
| `app/cli/import_verified_provincial_key_universities_batch_09.py` | 省属重点本科第 09 批导入命令；覆盖北京 12、天津 8 所具备双一流、市级高精尖/一流或高水平建设身份及生化环材、医药、食品或检测方向的公办普通本科，并记录既有、行业不符、公安、证据不足、职业和民办边界原因；长校名无法稳定命中时使用官网法定主校区地址。 |
| `app/cli/import_verified_public_security_universities.py` | 公安/公共安全本科专批命令；以教育部 2026 普通高校目录全量纳入 33 所公安、警察、刑事、司法警官和消防救援本科院校，保留 23 所专科警校为后续边界清单，并以低并发队列定位主校区；对南京/江苏、郑州/河南等同城易混淆校名使用官方门牌地址。 |
| `app/cli/import_verified_ordinary_undergraduate_universities_batch_01.py` | 普通公办本科第 01 批导入命令；从教育部 2026 高校底表筛选河北 27、山西 19 所具备生化环材、医药、食品农业或检测方向的剩余公办本科，固化 4 所财经/传媒院校的排除原因，并以低并发队列定位主校区；新更名的应急管理大学使用招生官网法定地址避免同名 POI 缺失。 |
| `app/cli/import_verified_ordinary_undergraduate_universities_batch_02.py` | 普通公办本科第 02 批导入命令；从教育部 2026 高校底表筛选内蒙古 11、辽宁 25 所具备生化环材、医药、食品农业或检测方向的剩余公办本科，固化 9 所财经/外语/艺术/职业本科边界原因，并以低并发队列定位主校区；新升格的朝阳师范学院使用招生章程法定地址。 |
| `app/cli/import_verified_ordinary_undergraduate_universities_batch_03.py` | 普通公办本科第 03 批导入命令；从教育部 2026 高校底表筛选吉林 13、黑龙江 15 所具备生化环材、医药、食品农业或检测方向的剩余公办本科，固化 10 所财经/艺术/职业本科边界原因，并复用低并发严格主校区 POI 队列定位。 |
| `app/cli/import_verified_ordinary_undergraduate_universities_batch_04.py` | 普通公办本科第 04 批导入命令；对账教育部 2026 高校底表与正式单位库，筛选上海 7、江苏 20、浙江 16 所具备生化环材、医药、食品或检测方向的剩余公办普通本科，逐校保留官网专业证据，固化 18 所财经/外语/政法/艺术/职业本科边界原因，并复用低并发严格主校区 POI 队列定位。 |
| `app/cli/import_verified_ordinary_undergraduate_universities_batch_05.py` | 普通公办本科第 05 批导入命令；对账教育部 2026 高校底表与正式单位库，筛选安徽 21、福建 9、江西 12、山东 19 所具备生化环材、医药、食品或检测方向的剩余公办普通本科，固化 27 所职业本科及纯财经/艺术/政法等边界原因，并复用低并发严格主校区 POI 队列定位。 |
| `app/cli/import_verified_ordinary_undergraduate_universities_batch_06.py` | 普通公办本科第 06 批导入命令；对账教育部 2026 高校底表与正式单位库，筛选河南 26、湖北 11、湖南 13、广东 5 所具备生化环材、医药、食品或检测方向的剩余公办普通本科，固化 42 所职业本科、合作办学及行业不符/证据不足边界原因，并复用低并发严格主校区 POI 队列定位。 |
| `app/cli/import_moe_university_directory.py` | 教育部 2026 年全国普通高校底表导入命令；复用通用官方来源服务分页读取 2,952 条记录，按 20 页输出进度，写入可追溯 `import_batch/import_row`，再分流为生环化材取证或体育例外取证队列。 |
| `app/cli/geocode_pending_sites.py` | 待编码地点任务入口；读取服务端 Web Service Key，批量创建可靠地图 pin；兼容高德“门牌号”和“门址”两种等价精确等级。 |
| `app/routers/health.py` | 无认证健康检查。 |
| `app/routers/insights.py` | 已登录主站用户可用的数据洞察聚合与 Excel 导出路由；把当前账号解析为负责范围或大区展开范围，并在服务调用前拒绝越权省市。 |
| `app/insights_schemas.py` | 数据洞察年度/季度、指标和范围模式枚举及聚合响应 DTO；响应明确可见省份/大区并返回大区聚合，金额保留 Decimal 字符串，实际销售与商机储备保持独立。 |
| `app/routers/auth.py` | 登录、退出和当前会话 API；当前会话摘要包含账号覆盖范围、关联销售人员、账号管理能力及销售数据库管理能力。 |
| `app/routers/authorized_users.py` | 仅超级管理员可用的授权账号安全列表、新增、销售关联/范围/状态修改和删除 API。 |
| `app/routers/admin_data.py` | 13 类后台数据的认证分页、外键选项及新增/修改/删除 API；路由把当前账号范围和操作者交给服务层，在写事务前完成目标记录及关联位置授权。 |
| `app/routers/admin_deals.py` | 受管理员会话保护的成交订单筛选、统一分页展示及优纳特订单级新增、修改、删除 API。 |
| `app/routers/admin_customer_groups.py` | 客户集团聚合列表、完整档案读取、原子新增、原子修改和级联删除的受保护 API。 |
| `app/routers/admin_salespeople.py` | 仅全国账号和超级管理员可用的销售人员聚合列表、完整档案读取、原子新增、原子修改和删除 API。 |
| `app/routers/organizations.py` | 账号范围内公开单位列表、筛选项和地图 pin，以及公开已成交客户地图、管理员完整单位 CRUD、审核与导出 API；公开单位链路显式注入当前账号，管理员账号保持全国。 |
| `app/routers/sales_office_locations.py` | 授权主站启用常驻点读取，以及受管理员角色保护的全量读取和字段修改 API。 |
| `app/routers/channel_partner_locations.py` | 授权主站渠道地图点读取，以及受管理员角色保护的完整字段读取和修改 API。 |
| `app/routers/customer_groups.py` | 授权主站账号范围内客户集团总部列表和单集团关系树详情 API；路由把当前账号范围交给服务层。 |
| `app/routers/competitors.py` | 授权主站同行主要据点列表和单同行市场详情 API；按当前账号覆盖范围过滤同行，非法、不可见或不存在 UUID 返回统一中文错误。 |
| `app/routers/admin_competitors.py` | 受管理员会话保护的同行聚合主列表 API；只处理分页、搜索和启用状态参数，聚合查询交给服务层。 |
| `app/routers/salespeople.py` | 授权主站销售覆盖聚合 API；区域账号只查询关联销售人员，全国账号和超级管理员查询全部，只接受 1、3、6、12 月。 |
| `app/routers/admin_typical_cases.py` | 受管理员会话保护的 31 省案例摘要、完整详情、同省成交项目候选、图片上传及原子 CRUD 路由。 |
| `app/routers/typical_cases.py` | 授权主站案例地图摘要和已发布案例详情路由；不返回草稿及内部关联字段。 |
| `app/typical_case_schemas.py` | 典型案例输入、管理摘要/详情、图片上传、公开地图和详情 Pydantic 合同；统一限制大陆省份、发布完整性、图片与指标数量。 |
| `app/main.py` | FastAPI 应用组合、CORS 与 `/api/v1` 路由注册。 |
| `alembic/` | Alembic 迁移环境；`20260827_0030` 把历史城市覆盖无损升级为四级销售覆盖范围，`20260827_0031` 新增账号四级范围并固定 `admin_syt` 超级管理员身份，`20260827_0032` 增加账号销售关联并回填区域演示账号。 |
| `Dockerfile` / `.dockerignore` | API 容器镜像与构建上下文控制。 |
| `tests/test_api_contracts.py` | 不依赖真实客户数据的 API 合同测试：存活/就绪检查、登录来源限制、权限、批量操作成功/输入失败/未授权路径、公开安全读取、管理员单位及关联商业记录原子新增/修改、失败事务回滚和输入约束。 |
| `tests/test_authorized_users_api.py` | 站点访问与授权账号 API 合同；覆盖普通用户拒绝、超级管理员安全摘要、四级范围及互斥校验、保留旧范围并新增范围的刷新顺序，以及账号 CRUD。 |
| `tests/conftest.py` | 为主站只读合同显式注入普通员工会话，保证测试不再依赖匿名业务接口。 |
| `tests/test_admin_data_api.py` | 统一管理员数据 API 合同测试；锁定 13 类资源白名单、未认证拒绝、区域账号禁止通用销售资源、Pydantic 输入失败，以及分页、新增、修改和删除成功路径。 |
| `tests/test_admin_deals_api.py` | 成交订单后台 API 合同测试；覆盖多产品成功响应、组合筛选传递、优纳特订单级增改删、金额校验和未授权拒绝。 |
| `tests/test_admin_competitors_api.py` | 同行聚合管理 API 合同测试；覆盖未认证拒绝、筛选透传和固定批量主列表汇总。 |
| `tests/test_admin_customer_groups_api.py` | 客户集团聚合管理 API 合同测试；覆盖未认证拒绝、单位树循环校验、固定批量汇总，以及完整档案读取、新增、修改和删除路径。 |
| `tests/test_admin_salespeople_api.py` | 销售人员聚合管理 API 合同测试；覆盖未认证拒绝、区域账号 403、内嵌范围校验，以及全国账号的完整档案读取、新增、修改和删除路径。 |
| `tests/test_sales_coverage.py` | 销售覆盖业务口径测试；锁定七个大区、四级范围归一和全国范围互斥规则。 |
| `tests/test_organization_query_indexes.py` | 单位大批量查询索引离线合同测试；锁定稳定分页复合索引和地点、证据、联系人、商机、成交项目外键索引。 |
| `tests/test_organization_integrity_constraints.py` | 单位并发完整性离线测试；锁定标准化名称、唯一主地点索引，以及数据库冲突回滚与安全中文 409。 |
| `tests/test_data_insights_demo_data.py` | 全国数据洞察虚构演示迁移的离线测试；锁定缺失十三省、三年季度分布、成交—商机关联和演示名称边界。 |
| `tests/test_heatmap_demo_rebalance.py` | 成交热力演示重排迁移的离线测试；锁定八家演示客户的一对一沿海目标、原始地点快照及演示标记/行数安全保护。 |
| `tests/test_account_access.py` | 账号范围离线测试；覆盖跨省并集、精确城市匹配、大区展开、全国范围、数据后台单位主列表 SQL、同行据点/成交客户准入 SQL 及越区写入 403。 |
| `tests/test_insights_api.py` | 数据洞察 API 合同测试；覆盖匿名拒绝、筛选成功、Decimal 序列化、Excel 导出、越权省份拒绝及大区展开。 |
| `tests/test_won_customer_service.py` | 已成交客户地图服务测试；锁定数据库筛选条件、实际成交项目排序和 `Decimal` 汇总口径。 |
| `tests/test_unite_heatmap_demo_data.py` | 优纳特热力图演示迁移离线测试；锁定成交/意向数量、跨省覆盖、五档合同金额和显式虚构命名。 |
| `tests/test_salesperson_models.py` | 销售人员离线模型测试：锁定覆盖层级约束、同范围多人覆盖边界、行政区/颜色约束、复合索引、销售归属外键与货币精度。 |
| `tests/test_salesperson_api.py` | 销售覆盖公开 API 测试；验证默认 3 月聚合、区域账号仅关联 Pin、未关联时安全空列表、非法月份中文 422 和月末自然月回溯边界。 |
| `tests/test_typical_case_api.py` | 典型案例 API 与模型合同测试；覆盖公开脱敏、管理员鉴权、31 省轻量列表、输入校验、创建成功、图片上传成功/伪造内容失败及数据库唯一约束。 |
| `tests/test_customer_group_models.py` | 客户集团离线模型测试：锁定独立表、货币精度、总部唯一索引及树/金额/坐标约束。 |
| `tests/test_customer_group_api.py` | 客户集团公开 API 和聚合测试：覆盖范围透传、总部懒加载、详情安全字段、非法 UUID、树层级、范围外节点剔除及可见金额汇总。 |
| `tests/test_competitor_models.py` | 同行模型离线测试：锁定独立表、金额精度、据点/区域/关系约束及正式单位多对多关系。 |
| `tests/test_competitor_api.py` | 同行公开 API 与关联投影测试：覆盖主要据点懒加载、20%/35%/45% 动态区域评分、强中弱门槛、无活动区域排除、安全字段、非法 UUID、已确认关联与未关联状态。 |
| `tests/test_organization_exports.py` | 单位 Excel 导出离线测试：验证审核列、地址、来源证据及公式注入防护。 |
| `tests/test_moe_university_directory_import.py` | 教育部高校目录导入的离线单元测试：验证七列解析、体育高校证据分流，以及首次专批遇到既有正式单位时的底表回写，不读取外网或真实数据库。 |
| `tests/test_verified_universities_batch_01.py` | 首批逐校官网取证名单的离线校验：验证官网证据、完整校址和审核标签。 |
| `tests/test_verified_double_first_class_universities.py` | 新增“双一流”差集离线校验：锁定 25 所纳入、6 所排除、官网证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_01.py` | 省属重点本科第 01 批离线校验：锁定 23 所范围、六个省级区域、官网证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_02.py` | 省属重点本科第 02 批离线校验：锁定华东三地 24 所范围、官网证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_03.py` | 省属重点本科第 03 批离线校验：锁定皖闽赣鲁 44 所范围、省份数量、排除项、官方证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_04.py` | 省属重点本科第 04 批离线校验：锁定豫鄂湘粤 49 所范围、省份数量、边界排除项、官方证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_05.py` | 省属重点本科第 05 批离线校验：锁定桂琼渝川 37 所范围、地区数量、既有/边界排除项、官方证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_06.py` | 省属重点本科第 06 批离线校验：锁定四川 8 所困难证据候选、逐校不纳入/暂缓原因、官方证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_07.py` | 省属重点本科第 07 批离线校验：锁定贵州 7、云南 8、西藏 2 所目标高校、边界排除原因、官方证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_08.py` | 省属重点本科第 08 批离线校验：锁定陕西 12、甘肃 7、青海 2、宁夏 1、新疆 5 所目标高校、边界排除原因、教育部底表来源、官方证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_provincial_key_universities_batch_09.py` | 省属重点本科第 09 批离线校验：锁定北京 12、天津 8 所目标高校、边界排除原因、教育部底表来源、公开证据和严格主校区 POI 检索输入。 |
| `tests/test_verified_public_security_universities.py` | 公安/公共安全本科专批离线校验：锁定 33 所本科、23 所专科边界、教育部官方目录证据、业务理由和严格主校区地理编码输入。 |
| `tests/test_verified_ordinary_undergraduate_universities_batch_01.py` | 普通公办本科第 01 批离线校验：锁定河北 27、山西 19 所目标高校、4 所财经/传媒边界、公开专业证据和严格主校区地理编码输入。 |
| `tests/test_verified_ordinary_undergraduate_universities_batch_02.py` | 普通公办本科第 02 批离线校验：锁定内蒙古 11、辽宁 25 所目标高校、9 所财经/外语/艺术/职业本科边界、公开专业证据和严格主校区地理编码输入。 |
| `tests/test_verified_ordinary_undergraduate_universities_batch_03.py` | 普通公办本科第 03 批离线校验：锁定吉林 13、黑龙江 15 所目标高校、10 所财经/艺术/职业本科边界、公开专业证据和严格主校区地理编码输入。 |
| `tests/test_verified_ordinary_undergraduate_universities_batch_04.py` | 普通公办本科第 04 批离线校验：锁定上海 7、江苏 20、浙江 16 所目标高校、18 所行业/职业本科边界、公开专业证据和严格主校区地理编码输入。 |
| `tests/test_verified_ordinary_undergraduate_universities_batch_05.py` | 普通公办本科第 05 批离线校验：锁定安徽 21、福建 9、江西 12、山东 19 所目标高校、27 所行业/职业本科边界、公开专业证据和严格主校区地理编码输入。 |
| `tests/test_verified_ordinary_undergraduate_universities_batch_06.py` | 普通公办本科第 06 批离线校验：锁定河南 26、湖北 11、湖南 13、广东 5 所目标高校、42 所职业/合作办学/行业边界、公开专业证据和严格主校区地理编码输入。 |
| `tests/test_official_sources.py` | 通用官方来源获取服务的离线单元测试：验证中文编码、传输中断重试、低并发页序与并发安全边界。 |
| `requirements.txt` | FastAPI、SQLAlchemy、Alembic、PostgreSQL 驱动、认证、PostGIS、Excel、Redis 与案例图片解码/转码所需 Pillow 依赖。 |
| `requirements-dev.txt` | 后端开发、测试、静态检查与浏览器自动化依赖，并通过 `-r requirements.txt` 引入运行时依赖。 |

## 数据库

数据库使用 Docker 内部网络的 PostgreSQL 16 + PostGIS，不发布数据库端口。`organization` 与 `competitor_customer` 分别拥有优纳特和同行订单主表；`sales_project_product`、`competitor_deal_product` 以级联外键保存任意多条产品名称、规格、单价、数量和产品总价，同行产品另保存可空图片。订单主表总价是合同/项目权威金额，不强制等于产品分项之和，以容纳折扣、税费和服务项。兼容期主表旧产品列只镜像第一条产品，业务读取与新写入均以产品明细表为准。

## 容器与网关

渠道网络由 `channel_partner_location` 独立持久化：管理接口可维护完整业务字段，授权主站地图接口仅投影名称、类型、展示坐标、半径与合作等级。当前 18 条演示记录的业务经纬度、授权区域、产品线、合同和备注均为空；地图所需的省会中心点保存在独立演示坐标列，避免把演示位置误当成已核验业务坐标。

| 路径 | 作用 |
| --- | --- |
| `docker-compose.yml` | 固定 Compose ASCII 项目名 `unite-sales-map`，在一个内部网络启动 PostGIS、Redis、FastAPI、Next.js 与 Nginx；Compose 显式固定 API“先 Alembic 升级、再启动 FastAPI”的生产入口，Web 使用镜像内已编译的生产产物且不挂载会遮蔽 `.next` 的匿名卷；仅网关向主机暴露 `.env` 的 `APP_PORT`。`frontend/public/cases` 作为 API 可写、Web 只读的共享案例图片目录挂载，同时将官方名单的低并发、重试和超时变量传入 API。 |
| `infra/nginx/default.conf.template` | 将 `/api/*` 转发给 FastAPI、将网页转发给 Next.js；按高德官方配置以 `/_AMapService/` 固定一级路由代理 Web 服务及自定义样式请求，网关追加安全密钥，浏览器不会获得该密钥。 |

## 文档与协作文件

| 路径 | 作用 |
| --- | --- |
| `docs/PROJECT_ARCHITECTURE.md` | 本文件：维护仓库结构、文件职责、已实现范围与规划边界。 |
| `docs/全国销售网络作战地图系统 - 需求说明书 V1.0 20260803.docx` | 业务需求说明书源文件。 |
| `docs/~$销售网络作战地图系统 - 需求说明书 V1.0 20260803.docx` | Office 打开 Word 文件时产生的临时锁定文件；关闭文档后可由 Office 自动移除。 |
| `AGENTS.md` | 全局开发约定，包括代码注释、文档同步、路径、测试、安全规则，以及 Ponytail、Caveman、Karpathy Guidelines 与 Awesome DESIGN.md 的每轮技能路由。 |
| `PRODUCT.md` | 产品定位、用户、能力边界与视觉原则，供设计和实现时参考。 |
| `README.md` | 实际环境变量、Docker 启动/停止、健康检查、本地校验与数据导入原则。 |

## 强制执行基线

- 本仓库唯一默认根目录为 `D:\桌面\优纳特销售网站`。所有终端操作以 `Set-Location -LiteralPath 'D:\桌面\优纳特销售网站'` 开始，不再使用 C 盘旧路径。
- 每轮执行、测试或代码生成前必须先完整读取根目录 `AGENTS.md`，再列出并检查本轮所需的应用、服务、环境变量与库；依赖缺失且无法安全自动安装时，停止实现并等待用户处理。
- 每次新增或修改源码、SQL、数据库迁移、配置、脚本或目录，都必须同步更新本文件的真实结构、文件职责与实现状态。若本文件未同步，变更视为未完成。
- 代码文件顶部和所有新增/修改的具名入口必须具有职责注释，重点说明“为什么存在、负责什么”；生成物与第三方文件除外。
- 用户级 Codex 技能安装在 `C:\Users\alien\.codex\skills\`，不复制进仓库或容器镜像。每轮由根 `AGENTS.md` 检查并路由：编码任务使用 Ponytail 与 Karpathy Guidelines，用户说明采用 Caveman `lite`，前端视觉任务使用 Awesome DESIGN.md；第三方可执行 hooks 默认禁用。

## 维护规则

首页单位热力图在地图容器内缩放居中，完整保留南部边界；选中省份使用独立 SVG 描边层且统计弹层置顶。地图框左下角的缩放控件以完整视图为默认和最小尺寸，可逐级放大、缩小并一键复位。销售常驻点以及经销商、代理商、合作伙伴覆盖层均默认隐藏，与热力图同步缩放，不能遮挡省份点击或统计弹层；合作等级只影响三类渠道覆盖层，不改变省级热力统计。

新增、移动、删除源码/素材/配置文件时，必须同时更新本文件对应条目。每个可维护的代码文件应在文件开头用一到两行说明模块职责及主要库；新增或修改具名函数、组件、路由处理器时，也应添加简短职责注释。生成文件（如 `next-env.d.ts`）、第三方依赖和二进制素材不手工添加注释。
