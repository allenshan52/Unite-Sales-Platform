# 优纳特销售数据可视化平台：协作规范

本文件约束在本仓库中工作的开发者和代码代理。开始任何任务前，先阅读本文件；如与用户明确要求冲突，以用户要求为准。

## 项目目标

构建一个面向内部管理员的销售数据可视化平台：Next.js 前端、FastAPI 后端、PostgreSQL 数据库。首版覆盖管理员登录、销售项目 CRUD、仪表盘、全国销售标点图、省级热力图及销售关系网。

## 工作目录与结构

项目根目录固定为 `D:\桌面\优纳特销售网站`。后续代码按以下结构组织：

```text
frontend/                 # Next.js 应用
backend/                  # FastAPI 应用
  app/                    # 路由、模型、schema、服务与配置
  tests/                  # 后端测试
  alembic/                # 数据库迁移
docs/                     # 架构、API 与运行文档
scripts/                  # 可重复运行的维护或初始化脚本
```

不要将业务代码直接放在仓库根目录。不要提交 `node_modules/`、`.next/`、`.venv/`、环境变量文件、数据库文件、测试覆盖率或构建产物。

## 环境与命令

- Python 虚拟环境固定使用根目录 `.venv`；Windows PowerShell 激活命令：`.\.venv\Scripts\Activate.ps1`。
- 后端依赖通过 `backend/requirements.txt` 管理；新增 Python 依赖后同步更新该文件。
- 前端依赖通过 `frontend/package.json` 管理；禁止手工修改 lockfile，使用包管理器安装后生成。
- PostgreSQL/PostGIS 通过根目录 Docker Compose 服务运行，不向主机发布数据库端口；连接配置只放在 `.env`，仓库仅维护 `.env.example`。
- 每个可运行子项目必须在其 README 或根 README 中提供开发、测试和构建命令。

## 后端约定

- 使用 FastAPI、SQLAlchemy、Pydantic 与 Alembic；API 路径统一以 `/api/v1` 开头。
- 路由层仅处理 HTTP 请求/响应；查询聚合放入 service 或 repository 层；模型、schema 与业务服务职责分离。
- 所有输入使用 Pydantic schema 验证，货币使用 `Decimal`/PostgreSQL `NUMERIC`，不使用浮点数保存金额。
- 数据库结构变更必须新增 Alembic migration；不要通过启动时建表替代迁移。
- API 返回统一、可读的中文错误消息；不得在响应或日志中泄露密码、令牌或连接字符串。
- 演示客户、联系人和项目必须是虚构数据，并通过注释或文档明确标记为演示数据。
- 对官方公开名单进行分页或批量抓取时，必须复用 `app.services.official_sources` 的低并发、重试与超时策略；禁止在 CLI 或业务服务中自行创建无上限线程池或直接绕过该策略发起网络请求。

## 前端约定

- 使用 TypeScript 严格模式、Next.js App Router 与 Tailwind CSS。
- 页面负责布局和组合，通用 UI 放在 `components/`，API 调用与类型放在 `lib/`；不得在多个组件复制请求或数据转换逻辑。
- 文案与日期、货币格式使用中文和中国地区格式；销售金额默认单位为人民币。
- 所有表单必须提供必填提示、范围校验、请求中状态和失败反馈；删除操作必须二次确认。
- 地图与图表数据必须由后端聚合 API 提供，前端不得硬编码业务统计结果。
- `AMap.MarkerCluster` 的 `renderMarker` 回调不得假设 `marker.getExtData()` 一定存在；必须通过稳定 ID/坐标索引回查数据，并在调用 `replaceAll` 等格式化方法前处理空值。
- 热力图默认按销售金额显示实际销售；仅在销售金额模式显示“实际/预计”图层选择。项目数量模式只展示项目数。

## 数据与安全

- 首版为单管理员。管理员初始账号和密码通过环境变量配置，密码只存储哈希值。
- 不提交真实客户信息、联系人信息、生产数据库导出或任何密钥。
- 预计销售与实际销售是独立指标，任何汇总 API 必须明确其口径，不能将预计金额计入实际销售额。
- 项目位置保留省、市、经纬度；首版地图按省级汇总。

## 质量门槛

- 变更后运行与改动相匹配的测试、类型检查或构建，并在交付说明中报告结果。
- 新增或修改 API 时，至少覆盖成功路径、输入校验失败和权限失败（如适用）。
- 新增或修改关键前端交互时，验证加载、空数据、错误和成功状态。
- 保持改动聚焦，不顺带重构无关文件；保留用户已有的未提交改动。
- 提交前检查 `git diff` 与敏感文件；提交信息使用简洁中文或 Conventional Commit 格式。

## 文档

- 根 README 说明项目用途、依赖安装、环境变量、数据库初始化与前后端启动方式。
- 重要的数据口径、地图交互和 API 约定更新到 `docs/`。
- 当实现与本规范或既定计划不一致时，先记录原因并征求用户确认。

## 每轮执行的强制启动检查

- **每一次调用命令、运行测试、生成或修改代码之前**，先完整读取本文件 `AGENTS.md`；不得仅凭上一次会话记忆执行。
- 项目唯一默认根目录固定为 `D:\桌面\优纳特销售网站`。所有 PowerShell 示例必须先执行：`Set-Location -LiteralPath 'D:\桌面\优纳特销售网站'`；禁止使用任何 C 盘旧路径。
- 在开始新任务前，先向用户列出本次所需的系统应用、服务、环境变量和新增库，并用只读命令检查实际可用性。涉及容器或数据库时，至少检查 `docker version`、`docker compose version`、`.env`、端口和数据库连接配置。
- 缺少依赖时，先说明缺失项和安装影响；能安全自动安装的，获得授权后安装并复查。需要用户完成 GUI 安装、管理员权限、账号登录、API Key 或外部网络授权时，必须停止，等待用户安装并验证完成后再生成依赖该组件的代码。
- 新增或修改源代码、SQL、迁移、配置、脚本或目录时，必须在同一轮完成三件事：补充文件/具名入口的职责注释、更新 `docs/PROJECT_ARCHITECTURE.md`、执行与改动匹配的验证。三者任一未完成，不得宣称实现完成。

## 全局技能路由

- 每轮读取本文件后，检查以下全局技能是否存在：`ponytail`、`caveman`、`karpathy-guidelines`、`awesome-design-md`（默认位于 `C:\Users\alien\.codex\skills\`）。任一缺失时先告知用户并停止依赖该技能的实现，不得静默跳过。
- 所有写代码、修复、重构、代码审查或依赖选择任务必须同时使用 `ponytail`（默认 `full`）与 `karpathy-guidelines`：先理解完整调用链和成功标准，再选择能满足明确需求的最小、聚焦改动；不得以“最小”为由省略安全、数据完整性、可访问性、注释、文档或验证。
- 面向用户的进度与交付说明默认采用 `caveman` 的 `lite` 原则：完整中文句子、无客套和重复内容。安全警告、不可逆操作确认、多步骤命令和可能产生歧义的说明必须恢复完整表达；代码注释、架构文档、提交信息和其他持久化文本保持正常专业文风。
- 所有前端设计、视觉重构、界面审查和设计系统任务必须使用 `awesome-design-md`，按需只读取最匹配的一到两套参考，不得一次加载全部参考，也不得照搬第三方商标、文案、图片或专有资产。纯后端、数据库和数据采集任务只完成存在性检查，不强行套用视觉参考。
- 第三方 skill 只按标准 `SKILL.md` 方式调用。未经用户另行明确授权，不启用仓库自带的 Node/Python hooks、MCP 服务或会话启动脚本。

## 代码注释与架构文档

- 每个可维护的源码或配置代码文件开头必须用一到两行注释说明模块职责，以及使用的主要框架或库；生成文件和第三方代码不手工修改。
- 每个新增或修改的具名函数、React 组件、路由处理器、服务方法或脚本入口，必须补充一到两行职责注释；注释解释“为什么/做什么”，不复述显而易见的语法。
- 新增、移动、删除源码、配置、素材或项目目录时，同步更新 `docs/PROJECT_ARCHITECTURE.md` 中的结构与文件职责；提交前检查文档是否仍与仓库一致。
- 根目录与命令示例统一以 `D:\桌面\优纳特销售网站` 为准，不使用已废弃的 C 盘项目路径。
